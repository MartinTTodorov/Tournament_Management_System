@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="right">

    @for (int i = 0; i<@Model.Tournaments.Count; i++)
    {
        <div class="tournament">

                <div class="tournament-text">
                    <span> placeholder</span>
                    <a href="/TournamentPage?ID=@Model.Tournaments[i].ID" class="tournament-title">@Model.Tournaments[i].Title</a>
                    <p>placeholder</p>
                    <a href = "/TournamentPage?ID=@Model.Tournaments[i].ID">View</a>
                </div>
        </div>
    }

</div>
